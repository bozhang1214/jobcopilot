# 职能族提示词包（packs）

按**职能**而非行业分层：同一职能（售前 / 产品 / 研发）在不同行业面对的 JD
结构高度相似，而不同职能即便同行业也差异巨大。

目录约定：

```
packs/<职能族>/<提示词文件名>.md
```

解析优先级：请求级 override → 宿主本地目录 → `packs/<职能族>/` → `base/`。

同名文件只需放**差异部分**的同名提示词即可覆盖 `base/` 里的通用版本，
不必整套复制。

> 本目录内容规划见 P1（首批：`presales` / `product` / `engineering`）。
